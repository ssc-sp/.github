name: Renovate Update Scan

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  renovate:
    runs-on: ubuntu-latest
    environment: renovate

    permissions:
      pull-requests: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: Run Renovate
        uses: renovatebot/github-action@6d859fc95779be83a0335ca704879b47e5d79641 # v46.1.16
        with:
          configurationFile: renovate.json
        env:
          RENOVATE_REPOSITORIES: ${{ github.repository }}

          RENOVATE_PACKAGE_RULES: >-
            [
              {
                "matchPackageNames": [
                  "aquasecurity/setup-trivy",
                  "aquasecurity/trivy-action"
                ],
                "datasourceTemplate": "github-releases"
              }
            ]

          RENOVATE_HOST_RULES: >-
            [
              {
                "hostType": "docker",
                "matchHost": "artifacts-artefacts.devops.cloud-nuage.canada.ca",
                "username": ${{ toJSON(steps.setup-jfrog.outputs.oidc-user) }},
                "password": ${{ toJSON(steps.setup-jfrog.outputs.oidc-token) }}
              },
              {
                "matchHost": "https://api.github.com/repos/aquasecurity/",
                "token": ""
              }
            ]
